# OpenRouter API client
requests>=2.32.3

# Output schema validation
pydantic>=2.0

# News scraping
feedparser>=6.0.11
beautifulsoup4>=4.12.3
lxml>=5.2.2          # Faster BS4 parser backend; falls back to html.parser if absent

# ----------------------------------------------------------------------------
# Setup note:
#
# Set your OpenRouter API key before running:
#   export OPENROUTER_API_KEY=sk-or-...          # Linux / macOS / Lambda env var
#   set OPENROUTER_API_KEY=sk-or-...             # Windows cmd
#
# Get a key at: https://openrouter.ai/settings/keys
# ----------------------------------------------------------------------------
# Core ML / inference — Gemma 4 E2B requires transformers >= 4.51
torch>=2.3.0
transformers>=4.51.0          # Gemma 4 support added in 4.51 (April 2026)
accelerate>=0.33.0
bitsandbytes>=0.43.0          # Only needed if use_4bit_quantisation=True

# News scraping
feedparser>=6.0.11
requests>=2.32.3
beautifulsoup4>=4.12.3
lxml>=5.2.2                   # Fast BS4 parser backend

# Scheduling
APScheduler>=3.10.4

# Install note:
# You must accept Google's Gemma licence on Hugging Face and be logged in:
#   huggingface-cli login
# The model will be auto-downloaded on first inference call.
#
# For GPU inference (recommended):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
